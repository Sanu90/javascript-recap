// let sanup = [1,2,3]

// sanup[4] = 10;

// console.log(sanup.length);

const user = {
    fName: "Pradeep",
    lName: "Kumar",
  };
  
  const print = (user) => {
    return user.fName;
  };
  console.log(print(user));
  
  
  