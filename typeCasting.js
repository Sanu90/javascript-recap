// Conversion of one data type into another data type is called as type casting.
// It is of two different types: Inplicit and explicit

// Implicit - It takes place automatically by interpreter or compiler when there is internal requirement

const greet = 'Hai ' + 'Admin';
console.log(greet);
const greet1 = 'good ' + 4;
console.log(greet1);
